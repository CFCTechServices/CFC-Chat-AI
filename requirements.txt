fastapi
uvicorn[standard]
gunicorn
pydantic
pydantic-settings
pinecone
sentence-transformers
python-docx
numpy
typing-extensions
python-dotenv
python-multipart>=0.0.20
pytest
supabase>=2.0.0
openai>=1.13.3
google-generativeai>=0.8.3
email-validator

openai-whisper

# ffmpeg is a system binary required by Whisper for audio/video processing.
# Install it on your OS (macOS: `brew install ffmpeg`, Ubuntu: `sudo apt install ffmpeg`).
# Add a Python helper that bundles ffmpeg binaries or provides a bridge:
imageio-ffmpeg>=0.4.8

# SSL certificate bundle — used by main.py and supabase_service.py for HTTPS
certifi

# scikit-learn — used by app/transcription/summarize_transcript.py for TF-IDF
scikit-learn
